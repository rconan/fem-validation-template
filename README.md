# FEM TEST REPORT TEMPLATE

Clone the GitHub repository and in a terminal run:
```
cargo run -- <full_path_to_FEM_test_repository>
```
and then compile the report with:
```
pdflatex fem_validation.tex
```